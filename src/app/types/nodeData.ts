import { CSSProperties } from 'react'

interface NodeData {
  icon?: string
  title?: string
  description?: string
  hideInputPort?: boolean
  hideOutputPort?: boolean
  style?: CSSProperties
}

export default NodeData
